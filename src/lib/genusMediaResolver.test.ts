// Contract-level checks for the Calyx-only Featured Genus client.
// The project does not currently expose a frontend unit-test harness in this branch,
// so these cases are recorded beside the resolver for the next test-harness build.

import { fetchCalyxGenusMedia } from './genusMediaResolver';

void fetchCalyxGenusMedia;
