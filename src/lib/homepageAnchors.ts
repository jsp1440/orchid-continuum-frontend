/**
 * homepageAnchors — stable ids the homepage sections hand off to each other.
 *
 * Kept out of the section components so a component can link to another
 * section without importing it.
 */

/** The first relationship the hero's thread leads to. */
export const FIRST_RELATIONSHIP_ANCHOR = 'first-relationship';

/** The Featured Genus exhibit. */
export const SPECIES_IN_FOCUS_ANCHOR = 'species-in-focus';

/** The public thematic Atlas. */
export const HOME_ATLAS_ANCHOR = 'home-atlas';
