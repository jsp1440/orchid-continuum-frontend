# Production queue reconciliation — 2026-09-21

Repository evidence checked against main `2f6f16191c376919649e02a791480064c5425517` and integration `623a3aa4691ce2f931491cf21d840b5004b595cb`. The live public issue inventory contains 22 open `oc-queued` issues. No issue was closed or relabeled during this pass: authenticated GitHub operations fail with HTTP 400 `Invalid MCP request metadata`, and the CLI has no GitHub write credential. Public GitHub reads and git fetch work.

This is a reconciliation of existing work, not a new autonomy campaign. No provider was called, no scientific record was mutated, and no deployment was performed. Render remains the only deployment target. Historical issue text requesting a different preview platform does not supersede that contract.

## Product changes prepared on `oc-production-queue-reconciliation`

| Issue | Existing implementation | Changes prepared against current main | Acceptance remaining |
| --- | --- | --- | --- |
| #676 | PR #677, integration merge `e9051a9194022aa264d8e4c6bb6e356fa694c1c9` | Converge the reviewed public readiness fix; also hide raw probe names and Add Plant instructions. Reject malformed/empty gates, contradictory readiness, and stale readiness during refresh. Preserve raw hook data for authorized tooling. | Publish the branch, exact-head CI, review and governed promotion. |
| #675 | PR #690, integration merge `d336fc1` | Converge the protocol disclosure, then finish the missing canonical `fetchFeaturedTaxonContinuum` read. Show documented conservation coverage/summary, separate unknown from unavailable, preserve the 80-character genus bound, reject noncanonical context and ignore locality/record URL fields. Cancel obsolete reads. | Publish, exact-head CI and browser acceptance. |
| #679 | PR #684, integration merge `d05cfe1` | Converge the development notice. Position it below the fixed navigation, retain session dismissal, add an explicit development badge, and disable unverified donation actions while keeping participation usable. The legacy fiscal-sponsor home page is not treated as a verified donation checkout. | Publish, exact-head CI and responsive browser acceptance; only the donation destination remains owner-configured. |

The integration branch contains older deployment/control-plane files, so it was not copied wholesale. Only the reviewed product slices above were brought forward, then repaired semantically.

## Queue decisions

“Partial” does not mean executable by the current validation-only worker. A merged integration PR does not prove current-main acceptance. Historical owner publication holds remain intact.

| Issue | Classification and evidence | Binding or narrow next implementation path |
| --- | --- | --- |
| #166 | Partial; PR #173 is closed without merge. Current main carries the newer canonical Featured Taxon implementation through #195/#231/#493: `featuredTaxonContinuum.ts`, `dailyGenusContext.ts`, `DailyGenusFeature.tsx`, `HomeSpeciesExhibit.tsx`. Preserve it instead of resurrecting the stacked legacy branch. Deployed media/cadence acceptance is outstanding. | Verified scope: `cap-homepage-featured-genus`. Reconcile the superseded PR and preserve the visual/publication hold before resuming acceptance. No paid narrative generation for validation. |
| #167 | Partial; PR #174 is closed. The graph's August census factory hard-coded UNKNOWN; there is no malformed persisted value or missing migration. `AtlasNextShell.tsx`, `questions.ts`, `useAtlasData.ts`, and `HomeAtlasContinuum.tsx` show a real but incomplete implementation. | `cap-atlas-next-thematic-5` is reconciled to PARTIAL in this branch, retaining its ID and later census IDs. Browser/deployed scores stay unknown. Finish the thematic homepage presentation/public-research separation, preserving the visual review hold. |
| #168 | Partial; PR #161 is closed. Canonical API/Atlas routing and the dependency readiness matrix exist on main; current Render/API-origin work is #717. A historical route fix alone does not satisfy the full homepage network audit. | Audit the mounted homepage calls and `dependencyReadinessData.ts`; add only missing contracts. No single existing leaf covers this entire cross-module audit. |
| #169 | Partial; PR #175 is closed. Main mounts one `ContinuumWeb` section under `AppLayout`, using the shared featured genus; the advanced Intelligence Graph is a distinct surface. | Continue public relationship accessibility/readability in `ContinuumWeb.tsx`. Do not bind this homepage task to the deeper Intelligence Graph leaf merely because both mention graphs. |
| #170 | Partial; PR #176 is closed. Main has `PublicCalyxGuide`, `featuredTaxonNavigation`, and `AtlasAwareCalyxRoute` with non-evidentiary genus context. Full section/theme/context acceptance is not proven. | Verified scope: `cap-homepage-public-calyx`. Reuse the canonical `/calyx` handoff, not the obsolete stacked homepage route. Preserve the publication hold. |
| #188 | Partial; canonical `/species/:slug`, `resolveFederatedSpecies`, source receipts and partner provenance exist. PR #90 and subsequent federation work are the implementation lineage. The requested responsive meeting path and screenshot package are not proved by source inspection. | Continue `SpeciesDossier.tsx` and its existing federation/evidence gates. The whole meeting deliverable spans both gates; do not force one broad binding or invent partner permissions. |
| #189 | Partial; `ResearchCenter.tsx` and `ResearchStationWorkbench` preserve taxon/project context and refuse an unrelated project. A project starter and partner-aware evidence presentation are a broader deliverable. | Bounded project-starter UI using existing project APIs. Do not substitute `domain-research-station-cap-15`'s unscored census for a scored implementation gate, or duplicate #188's partner panel. |
| #610 | Partial; main has account-scoped offline drafts; PR #626 is the first slice. Integration PR #693 adds the governed upload path. `CalyxField.tsx` on main still explicitly says upload is unavailable. | Converge #693 and its privacy/idempotence contracts, then complete edit/reopen/export/media behavior. Add a real Field Journal capability to the graph; device acceptance stays separate. |
| #664 | Real external acceptance dependency; the issue explicitly requires Lance's actual devices and independent repetition of field/research workflows. Automated tests cannot satisfy it. | Preserve the real-user acceptance task. Prepare the guide/journey evidence from actual functionality; no fake device receipt or arbitrary graph binding. |
| #675 | Partial, with a verified reviewed implementation and missing canonical read repaired locally; see product table. | Narrow Atlas-to-Conservation evidence handoff. The existing Conservation census is broader and unscored; it needs decomposition, not a guessed binding. |
| #676 | Implemented in integration but missing on main; reviewed code plus additional leak/fail-closed repairs prepared locally. | Verified scope: `cap-conservatory-readiness-gate`. Attach #677 lineage and validate promotion; do not start a second implementation. The attempted explicit binding failed at GitHub transport. |
| #679 | Partial; reviewed PR #684 is merged into integration, absent main. This branch fixes the remaining public notice/CTA defects. | A public-launch notice/participation acceptance leaf with the actual implementation evidence. There is no existing dedicated launch leaf to bind honestly. |
| #680 | Partial; PR #686 adds `/community` in integration, including observation provenance. This does not complete topic rooms, private conversations, moderation/appeals, or explicitly metered Calyx participation. | Converge the observation/intake slice, then implement separately bounded community/moderation contracts. The greenhouse-monitoring leaf is not a community-chat binding. |
| #681 | Partial; PR #685 supplies newsletter/contact pages in integration. A UI is not proof of a mailbox identity, authorized sending, editorial publication, or live intake. | Converge the existing pages and backend #1435 contract. Discover existing account capabilities without secrets; preserve editorial send/ingestion gates. No exact existing communications graph leaf. |
| #682 | Partial and broad; project/workspace/display-preference primitives exist. A persisted modular scientific notebook and account-metered public Calyx are not proved by those components. | Begin with a bounded deterministic private notebook/persistence/export contract. Do not treat the development Budget Governor as a user billing ledger, or bind the entire scope to a Calyx UI leaf. |
| #683 | Partial; closed PR #689 and subsequent integration Deception Lab/hypothesis work are the recovery lineage. Main has no mounted Deception Lab workspace. | Converge reviewed lab/hypothesis components and explicit Field/Atlas/Literature contracts. The pollinator data-reader leaf is not the whole lab. Camera hardware remains an extension until verified. |
| #289 | Existing open PR #647; explicit publication hold. | Retain that PR and its hold; no duplicate card-accessibility implementation. |
| #296 | Initial schema/scoring/census implemented on main by #298 (`c8238e7`), consumed by Observatory and scheduler, with structural/scoring/evidence tests. Some broader requested fields/census refinements remain separate from the initial executable layer. | Candidate for a scoped completion receipt after matching remaining criteria against current data; do not automatically close the whole issue from the closed #303 reference alone. |
| #308 | Partial; #317 supplies `legacy-registry`, but many entries still have null source commits and explicitly unverified source evidence. Later recovered education surfaces make some historical absence notes stale. | Read-only source verification and registry reconciliation; existence of the registry is not completed archaeology. Preserve closed #312 lineage. |
| #523 | Existing open held #531 plus integration #691; scientific/security owner boundary. | Reconcile the audit evidence through that lineage. Do not remove the hold or activate taxonomy. |
| #662 | Budget/lease/shared-context/router code exists through #663 and later main safeguards. The issue's paid-provider demonstration stages conflict with the current explicit no-provider authorization. | Settle only verified implementation scope; leave paid demonstrations gated. Do not spend to close the ticket. |
| #703 | PR #705 and #706/#708 reasoning-map fixes are in integration, absent main. | Converge the complete reviewed reasoning-map lineage and locality fixes; do not rebuild or close from #705 alone. |

## Control defects repaired

- `lineageFor` now recognizes explicit affirmative “Implements #…” declarations, including same-repository qualified references. Negative prose, other repositories, incidental mentions and issue-number prefixes do not establish that lineage.
- Inventory preserves a merged PR's target branch. A merge into integration now asks for current-main/acceptance reconciliation instead of claiming the product work is already complete.
- #167's census entry is scored from current source, with partial implementation and no invented browser or deployment evidence. Its stable binding remains `cap-atlas-next-thematic-5`.

## Execution capabilities and next publication step

The current provider-free registry runs tests, lint, typecheck, deployment validation, build and a route sweep. It cannot author a product patch, push an implementation branch or create a product PR. Assigning those validation capabilities to an unfinished development issue would produce a validation receipt, not implementation. PR #709 separately documents that the route sweep also lacks its browser/runtime setup in the hosted worker. The #608 subscription-worker preflight checks authentication but is not a substitute for a complete fenced product worker; its no-API boundary remains unchanged.

The concrete work prepared here is the three product slices above plus queue fixes, ready for authenticated branch publication and exact-head CI. GitHub label/PR writes are the immediate external blocker. Once access is restored, publish this branch as a convergence of the existing product PRs, attach their lineage, inspect exact-head checks and the required UI acceptance, then promote only the accepted slices under existing owner gates. Bind the verified nodes only with the implementation/acceptance scope and a real supported executor. A validated command run alone must not close a product issue.

No new lease was acquired, no autonomous implementation was claimed, and no queue item was marked done during the outage. The failed browser binary download is recorded as a validation limitation, not a product failure or a passing browser check.
