name: Orchid Deterministic Dispatch

on:
  workflow_call:
    inputs:
      issues_json:
        required: true
        type: string
      wave_hash:
        required: true
        type: string
      artifact_name:
        required: true
        type: string

permissions:
  contents: write
  issues: write
  pull-requests: write
  actions: write
  id-token: write

jobs:
  context-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node scripts/verify-autonomy-context.mjs

  lane:
    needs: context-contract
    strategy:
      fail-fast: false
      max-parallel: 8
      matrix:
        issue: ${{ fromJSON(inputs.issues_json) }}
    uses: ./.github/workflows/orchid-budgeted-completion-lane.yml
    with:
      issue_number: ${{ format('{0}', matrix.issue) }}
      wave_hash: ${{ inputs.wave_hash }}
      artifact_name: ${{ inputs.artifact_name }}
      provider_authorized: false
